@model EFBook.PUBLISHER

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>PUBLISHER</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.Publisher_ID)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Publisher_ID)
            @Html.ValidationMessageFor(model => model.Publisher_ID)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.Publisher_Name)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Publisher_Name)
            @Html.ValidationMessageFor(model => model.Publisher_Name)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
